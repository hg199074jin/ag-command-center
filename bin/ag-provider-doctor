#!/usr/bin/env python3
"""ag-provider-doctor — 探测 codex/claude 能力，写 provider-capabilities.json。

只运行 `--version` / `--help`，无副作用。建议 provider 升级后重跑。
"""

import os
import sys

sys.path.insert(0, os.path.expanduser("~/.local/share/ag"))

from runtime import cli  # noqa: E402

if __name__ == "__main__":
    sys.exit(cli.main_probe(sys.argv[1:]))
